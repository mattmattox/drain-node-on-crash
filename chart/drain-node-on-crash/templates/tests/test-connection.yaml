apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "drain-node-on-crash.fullname" . }}-test-connection"
  labels:
    {{- include "drain-node-on-crash.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "drain-node-on-crash.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
