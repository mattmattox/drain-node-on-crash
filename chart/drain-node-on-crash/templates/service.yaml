apiVersion: v1
kind: Service
metadata:
  name: {{ include "drain-node-on-crash.fullname" . }}
  labels:
    {{- include "drain-node-on-crash.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drain-node-on-crash.selectorLabels" . | nindent 4 }}
